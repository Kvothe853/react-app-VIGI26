import styled from 'styled-components';

const StyledLogsContainer = styled.div`
  padding: 25px;
`;

const LogsData = (props) => {
  return <StyledLogsContainer>Hello</StyledLogsContainer>;
};

export default LogsData;
